# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# secrets
.env
*.key

# caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# scratch / temp (不进 git)
scratch/

# OS
.DS_Store

# Notion 坐标(私人工作区, 不进公网)
data/notion_ids.json

# Notion 私人工作区脚本(含page id坐标,只留内部端)
scripts/verify_notion.py
scripts/create_notion_db.py
scripts/write_samples_to_notion.py
scripts/write_full_to_notion.py
scripts/build_ssot_kol_list.py
scripts/fix_ssot_bio_alive.py
scripts/fix_ssot_bio_round2.py

# Notion KOL List 镜像(顶层 _notion_db 是私人工作区 database id,只留内部端)
data/kol_list_ssot.json

# 删人操作的滚动备份(体积大且为派生产物,只留内部端)
data/_removed_backup/

# 发布脚本含内部环境标识,只留内部端
scripts/publish.sh
data/batch_daily.json

# 翻译脚本的滚动备份(派生产物, 不进 git)
*.qbak
*.ml.bak
*.retry.bak
*.i18n.bak
