# Forecast-Checker 会话交接（2026-08-22 11:10 JST）

> 用途：当前会话 token 过多，压缩状态供新会话接手。
> 新会话读完本文件即可继续，无需回溯历史对话。

---

## 一、项目当前稳定态（已验证，非估计）

| 项 | 值 |
|---|---|
| SSOT `data/backfill_full.json` | **98 人 / 556 条**，detail 覆盖约 98% |
| Notion DB | 98 行（`3c047eb5-fd3c-8181-b3fc-db5ade59e3b8`） |
| 公网 dashboard | 98 卡片 / 556 条 |
| 个人端 commit | `65951f6`（`Curarpikt0000/Forecast-Checker` main） |
| 内部端 commit | `c2a4a27d`（ChaoProjects monorepo，分支 `chao.jin/n2gr-personas-restaurant-v1-handoff-20260805`） |
| 一致性断言 | `python3 scripts/check_consistency.py` → **全绿** |

身份类型 **8 类**（2026-08-22 新增第 8 类）：
🔮灵媒通灵 / ✨占星预言 / 📜预言先知 / 👁遥视RV / 🌌出体OBE / 🧪预知研究 / 📊模型预测者 / **☯金融玄学·术数预测**

领域 **9 类**（新增「金融市场」）。

---

## 二、进行中的任务（最高优先级）

### Chao 的指令原文（2026-08-22 11:0x）
> Amanda Grace 我们 Wiki 里面应该有这样的范例：这种 anonymous people 是一定要避免的。
> 我们要利用 Notion 的 QLDB 去抓到准确的人名，并且每天都要利用准确的人名去抓取，
> 进而更新 DB、GitHub 以及 Dashboard。这种人名被 redact 掉的情况一定要避免。

### 已查实的事实（务必先读，避免重复排查）

**1. 磁盘基本是干净的，绝大多数 `ANONYMIZED_*` 只是显示层脱敏。**
```bash
python3 -c "
import json;d=json.load(open('data/backfill_full.json',encoding='utf-8'))
s=[p for p in d['people'] if p['id']=='amanda_grace'][0]
print(s['display_name'].encode('utf-8'))"   # -> b'Amanda Grace'  真名，没坏
```
所以「Amanda Grace 被 redact」是**我在终端看到的显示效果**，不是数据损坏。
判定铁律：**看字节，不看终端回显**；`grep -c ANONYMIZED <file>` 才是真值检验。

**2. 但确实有 2 条真污染（必须修）**
```
grep -c 'ANONYMIZED' data/backfill_full.json   # -> 2
```
- `bopolny` — predictions 某条 detail 正文含 `Neil Howe`
- `andrewpancholi` — predictions 某条 detail 正文含 `dispatched earlier`

特征：**被脱敏的是 detail 正文里引用的第三方人名，不是人物主名**。
源头文件：`data/batch_esoteric_finance.json`（今日从 Eco 迁入的 11 人批次），
该文件本身 `grep -o 'ANONYMIZED_[A-Z_]*[0-9]*' | sort | uniq -c` → `2 Craig Hamilton-Parker`。

**修法**：改 SSOT 源文件 `data/batch_esoteric_finance.json`（**不是** `backfill_full.json`，那是派生产物会被 cron 冲掉），
回源核实那两条 detail 引用的真实人名后回填，再 `merge_backfill.py` + `build_dashboard.py` + `sync_notion_full.py`。
若核实不到真名，改用描述性称呼（如「一位美国周期理论研究者」），**绝不猜测还原**。

### 待办（按 Chao 指令拆解）— 已全部完成 2026-08-22
1. ~~修 2 条污染~~ **已完成**：回源核实后回填真值（一条经官网三人并列句式双源交叉确认；
   另一条实为犹太新年节期名被 redactor 误判成人名）。
2. ~~查 Wiki 现有范例~~ **已完成**：此前只有零散提及、无专页，已新建
   `knowledge/engineering/redactor-anonymized-placeholder-trap.md`。
3. ~~建立防污染机制~~ **已完成，共三道防线**：
   - **入库前断言**：`check_consistency.py` 的 `[2] 脱敏污染检查`，扫 `data/` 下所有 JSON，
     有 `ANONYMIZED_` 即 FAIL。已挂 daily cron step 6。
   - **采集入口断言**：`export_targets_from_notion.py` 若发现 Notion 姓名含占位符，
     拒绝该人并 `exit 1` 阻断当日采集。**已用测试桩实测验证 exit=1 生效**
     （注意：构造测试用的占位符字面量会被 redactor 替换，须用 `bytes.fromhex(...)` 构造）。
   - **采集侧铁律**：子 agent 任务描述里的「占位符不猜不补，改描述性称呼」。
4. ~~Notion QLDB 作人名权威源~~ **已完成**：Chao 确认用本项目 DB `3c047eb5-...`。
   新增 `scripts/export_targets_from_notion.py`，cron step 1 已从
   `export_active_targets.py`（读本地文件，已废弃）切换为它。

### 顺带修掉的一个死循环 bug
`sync_notion_full.py` 原逻辑 `if not preds: status = "历史复核"` ——
把「在世但尚未采到内容」的人标成历史复核，而 `export_targets_from_notion.py` 只收「在世」，
于是这些人**永远进不了每日采集**，也就永远采不到内容。
已改为：状态只由 `alive` 决定；「历史复核」仅留给已故者或 `status` 字段明确写了无新内容的在世者。
修复后状态分布：在世 80 / 已故 17 / 历史复核 1（仅 McMoneagle，正确）。
daily cron 覆盖 70 → **80 人**（11 位新迁入者全部纳入，含 3 个此前被误判的中文易经号）。

---

## 三、今日已完成（勿重做）

1. detail 补全：441 → 512 条（curl 原始 HTML 绕过 `web_extract` 的 LLM 摘要截断，找回 4 条被误判「查不到」的）。
2. 空壳人物补全：0 条人物 13 人 → 1 人（仅 McMoneagle，有明确「历史复核」status）；新增 71 条全部带 source_url。
3. 修数据错误：`robert_bruce` 名册标 `alive=true`，实际 2024-04-22 去世（其官网 "The late Robert Bruce" 公告佐证）→ 改 `batch_5.json` / `roster_candidates.json` 的 `alive=false`、`years=?-2024`，并移出 `daily_targets`。
4. 从 Eco 迁入 11 位金融玄学/术数派（Chao 今日裁定），新增第 8 身份类 + 第 9 领域。
5. 建 `scripts/check_consistency.py` 三处一致性断言，挂进 daily cron **step 6（必跑）**。
6. Notion 全量同步 98 行；双端 push；ChaoWiki 归档 3 页知识。

---

## 四、关键脚本与用法

```bash
cd ~/Projects/Forecast-Checker

# 全流程
python3 scripts/merge_backfill.py        # 源文件 -> backfill_full.json（派生产物）
python3 scripts/build_dashboard.py       # -> dashboard/index.html
cp dashboard/index.html index.html       # 根 index 供 GitHub Pages
python3 scripts/sync_notion_full.py      # 全量同步 Notion（分页清空+重写，脏数据自愈）
python3 scripts/check_consistency.py     # 三处一致性断言（--no-web 可跳过公网）
```

### 硬编码枚举有 3 处，新增类别必须同时改（否则新人物静默消失）
1. `scripts/merge_backfill.py` 的 `_person_types` / `_topic_domains`（约 247-248 行）
   —— **最隐蔽**：不改则导航栏不生成该分类入口，人在库里但页面找不到。
2. `scripts/build_dashboard.py` 的 `PTYPE_META` / `DOMAIN_COLOR`（约 24-35 行）—— 不改则落默认灰色。
3. Notion DB 的 select / multi_select 选项 —— 不改则写入 **400**。
   `check_consistency.py` 的 `PTYPE_OK` / `DOMAIN_OK` 会前置拦截这种情况。

### 源文件白名单
新增数据必须建源文件并挂进 `merge_backfill.py` 第 68 行的文件列表；
`_MERGE_APPEND` 元组内的文件走「按 id 合并 predictions 且按 summary 去重」语义。
**绝不直接写 `data/backfill_full.json`**（派生产物，cron 重建会冲掉）。

---

## 五、Wiki 相关（待确认 Chao 指的是哪页）

ChaoWiki（`~/Projects/ChaoWiki`，内网 `code.uber.internal:not-production/ChaoWiki`）中
提到 redactor / 脱敏的页面：
- `knowledge/engineering/mcp-gateway-proxy-auth-404-diagnosis.md`（含 redactor 敏感串陷阱、/proc 读值绕过）
- `knowledge/agent-rules/subagent-batch-orchestration.md`（今日新建，含「人名占位符不猜不补」铁律）
- `knowledge/crawler/web-extract-truncation-raw-html-bypass.md`（今日新建）
- `knowledge/engineering/uber-objectconfig-service-land-pitfalls.md`
- `knowledge/engineering/uber-hermes-web-search-stack.md`
- `knowledge/agent-rules/rtk-usage.md`
- `knowledge/engineering/oauth-local-callback-terminal-state.md`

今日已 push 至 ChaoWiki `b68e7db`：
- `knowledge/engineering/derived-artifact-rebuild-idempotency.md`（新）
- `knowledge/agent-rules/subagent-batch-orchestration.md`（新）
- `knowledge/engineering/notion-dedup-fail-loud.md`（增补 Notion 批量写两坑）

**注意**：ChaoWiki 工作区有其他会话的未提交改动（`tooling/marketing-systems-access/`），
pull 前先 `git stash` 隔离，pull 后 `pop` 恢复；提交只 `git add` 自己的文件路径。

---

## 六、纪律提醒（本项目踩过的坑）

- **ChaoProjects 是 monorepo，绝不 `git add .`**；只 `git add Forecast-Checker/`。
- 内部端 push 前 `source ~/.certenv`；commit 用 `-c commit.gpgsign=false --no-gpg-sign`；commit message 禁括号。
- 个人端 push 前跑 red-line grep：
  `grep -inE 'code\.uber\.internal|uberinternal|aifx|presto|\.certenv|chao\.jin|ChaoProjects'`
- 改任何数据文件前备份到 `scratch/backups/<name>.<ts>.json`。
- **幂等 ≠ 正确**：转换脚本若拿派生产物做去重基准，第二次运行会把自己上轮成果判为「已存在」导致源文件缩水；
  修完要回滚到污染前备份重跑，验证绝对数字（详见 ChaoWiki `derived-artifact-rebuild-idempotency`）。
- daily cron `683f038fb51c` 每天 09:20 JST 跑，会持续增量——**人工对数永远追不上，以断言脚本为准**。
  cron 定义备份在 `/home/user/.hermes/cron/jobs.json.bak.20260822`。
- **绝不编造**：查不到就留空 / 标 status，不猜人名、不臆造日期出处。
